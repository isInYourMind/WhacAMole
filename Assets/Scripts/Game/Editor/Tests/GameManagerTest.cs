using NSubstitute;
using NUnit.Framework;

namespace Game.Editor.Tests
{
    [TestFixture]
    public class GameManagerTest
    {
        [SetUp]
        public void Setup()
        {
            // var gameManager = Substitute.For()
        }

        [Test]
        public void Create_Room()
        {
            Assert.IsTrue(true);
        }
    }
}